# Verification Methodology

- Directed sequence testing for an overlapping `1011` Moore detector.
- The testbench counts detector pulses and checks the expected number of sequence hits.
- Reset behavior is exercised before stimulus.
- Optional VHDL analysis runs automatically when `ghdl` is installed.
