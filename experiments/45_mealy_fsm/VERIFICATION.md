# Verification Methodology

- Directed sequence testing for an overlapping `1011` Mealy detector.
- The testbench samples combinational detect pulses during input transitions.
- Reset behavior is exercised before stimulus.
- Optional VHDL analysis runs automatically when `ghdl` is installed.
