# Verification Methodology

- Randomized testing covers 1000 operand triples.
- A directed maximum-value test checks the required 10-bit sum width.
- Scoreboard computes the golden sum directly in the testbench.
- Optional VHDL analysis runs automatically when `ghdl` is installed.
